import { useEffect, useState } from "react";
import { Link } from "react-router-dom";

export function SlidePreviewer({ slide }) {
    // Purpose of this function should take the intermediate JSON and convert it into a preview format
}